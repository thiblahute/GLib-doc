fragment_downloaded_cb({"url": "running.html#systemtap", "fragment": "SystemTap \nSystemTap is a dynamic whole system analysis toolkit. GLib ships with a file libglib so. stp which defines a set of probe points which you can hook into with custom SystemTap scripts. See the files libglib so. stp libgobject so. stp and libgio so. stp which are in your shared SystemTap scripts directory. \n"});