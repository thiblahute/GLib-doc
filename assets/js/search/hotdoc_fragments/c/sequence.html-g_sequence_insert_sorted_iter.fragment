fragment_downloaded_cb({"url": "c/sequence.html#g_sequence_insert_sorted_iter", "fragment": "g_sequence_insert_sorted_iter \nLike g_sequence_insert_sorted but uses a GSequenceIterCompareFunc instead of a GCompareDataFunc as the compare function. \niter_cmp is called with two iterators pointing into seq. It should return if the iterators are equal a negative value if the first iterator comes before the second and a positive value if the second iterator comes before the first. \nIt is called with two iterators pointing into seq. It should return if the iterators are equal a negative value if the first iterator comes before the second and a positive value if the second iterator comes before the first. \n"});