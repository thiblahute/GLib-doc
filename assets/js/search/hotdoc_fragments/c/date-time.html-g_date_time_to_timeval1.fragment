fragment_downloaded_cb({"url": "c/date-time.html#g_date_time_to_timeval1", "fragment": "g_date_time_to_timeval \nStores the instant in time that datetime represents into tv. \nThe time contained in a GTimeVal is always stored in the form of seconds elapsed since UTC regardless of the time zone associated with datetime. \nOn systems where long is bit ie all bit systems and all Windows systems a GTimeVal is incapable of storing the entire range of values that GDateTime is capable of expressing. On those systems this function returns FALSE to indicate that the time is out of range. \nOn systems where long is bit this function never fails. \n"});