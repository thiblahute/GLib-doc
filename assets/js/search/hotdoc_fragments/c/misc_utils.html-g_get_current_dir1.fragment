fragment_downloaded_cb({"url": "c/misc_utils.html#g_get_current_dir1", "fragment": "g_get_current_dir \nGets the current directory. \nThe returned string should be freed when no longer needed. The encoding of the returned string is system defined. On Windows it is always UTF \nSince GLib this function will return the value of the PWD environment variable if it is set and it happens to be the same as the current directory. This can make a difference in the case that the current directory is the target of a symbolic link. \n"});