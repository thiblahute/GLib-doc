fragment_downloaded_cb({"url": "c/sequence.html#g_sequence_insert_sorted1", "fragment": "g_sequence_insert_sorted \nInserts data into sequence using func to determine the new position. The sequence must already be sorted according to cmp_func otherwise the new position of data is undefined. \ncmp_func is called with two items of the seq and user_data. It should return if the items are equal a negative value if the first item comes before the second and a positive value if the second item comes before the first. \n"});