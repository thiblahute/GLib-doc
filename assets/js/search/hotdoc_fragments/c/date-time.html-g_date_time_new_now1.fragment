fragment_downloaded_cb({"url": "c/date-time.html#g_date_time_new_now1", "fragment": "g_date_time_new_now \nCreates a GDateTime corresponding to this exact instant in the given time zone tz. The time is as accurate as the system allows to a maximum accuracy of microsecond. \nThis function will always succeed unless the system clock is set to truly insane values or unless GLib is still being used after the year \nYou should release the return value by calling g_date_time_unref when you are done with it. \n"});