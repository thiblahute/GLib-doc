fragment_downloaded_cb({"url": "c/unicode.html#g_unichar_break_type1", "fragment": "g_unichar_break_type \nDetermines the break type of c. c should be a Unicode character to derive a character from UTF encoded text use g_utf8_get_char The break type is used to find word and line breaks text boundaries Pango implements the Unicode boundary resolution algorithms and normally you would use a function such as pango_break instead of caring about break types yourself. \n"});