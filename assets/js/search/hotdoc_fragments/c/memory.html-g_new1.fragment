fragment_downloaded_cb({"url": "c/memory.html#g_new1", "fragment": "g_new \nAllocates n_structs elements of type struct_type. The returned pointer is cast to a pointer to the given type. If n_structs is it returns NULL. Care is taken to avoid overflow when calculating the size of the allocated block. \nSince the returned pointer is already casted to the right type it is normally unnecessary to cast it explicitly and doing so might hide memory allocation errors. \n"});