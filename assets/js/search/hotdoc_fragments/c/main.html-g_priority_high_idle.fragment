fragment_downloaded_cb({"url": "c/main.html#g_priority_high_idle", "fragment": "G_PRIORITY_HIGH_IDLE \nUse this for high priority idle functions. \nGTK uses G_PRIORITY_HIGH_IDLE for resizing operations and G_PRIORITY_HIGH_IDLE for redrawing operations. This is done to ensure that any pending resizes are processed before any pending redraws so that widgets are not redrawn twice unnecessarily. \n"});