fragment_downloaded_cb({"url": "c/testing.html#g_assert_cmpmem1", "fragment": "g_assert_cmpmem \nDebugging macro to compare memory regions. If the comparison fails an error message is logged and the application is either terminated or the testcase marked as failed. \nThe effect of g_assert_cmpmem m1 l1 m2 l2 is the same as g_assert_true l1 l2 memcmp m1 m2 l1 The advantage of this macro is that it can produce a message that includes the actual values of l1 and l2. \n"});