fragment_downloaded_cb({"url": "c/sequence.html#g_sequence_sort_iter1", "fragment": "g_sequence_sort_iter \nLike g_sequence_sort but uses a GSequenceIterCompareFunc instead of a GCompareDataFunc as the compare function \ncmp_func is called with two iterators pointing into seq. It should return if the iterators are equal a negative value if the first iterator comes before the second and a positive value if the second iterator comes before the first. \n"});