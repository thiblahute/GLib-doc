fragment_downloaded_cb({"url": "c/unicode.html#g_utf8_prev_char1", "fragment": "g_utf8_prev_char \nFinds the previous UTF character in the string before p. \np does not have to be at the beginning of a UTF character. No check is made to see if the character found is actually valid other than it starts with an appropriate byte. If p might be the first character of the string you must use g_utf8_find_prev_char instead. \n"});