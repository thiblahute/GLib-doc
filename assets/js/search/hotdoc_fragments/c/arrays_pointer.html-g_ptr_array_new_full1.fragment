fragment_downloaded_cb({"url": "c/arrays_pointer.html#g_ptr_array_new_full1", "fragment": "g_ptr_array_new_full \nCreates a new GPtrArray with reserved_size pointers preallocated and a reference count of This avoids frequent reallocation if you are going to add many pointers to the array. Note however that the size of the array is still It also set element_free_func for freeing each element when the array is destroyed either via g_ptr_array_unref when g_ptr_array_free is called with free_segment set to TRUE or when removing elements. \n"});