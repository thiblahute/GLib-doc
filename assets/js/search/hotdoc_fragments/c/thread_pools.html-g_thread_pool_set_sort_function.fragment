fragment_downloaded_cb({"url": "c/thread_pools.html#g_thread_pool_set_sort_function", "fragment": "g_thread_pool_set_sort_function \nSets the function used to sort the list of tasks. This allows the tasks to be processed by a priority determined by func and not just in the order in which they were added to the pool. \nNote if the maximum number of threads is more than the order that threads are executed cannot be guaranteed Threads are scheduled by the operating system and are executed at random. It cannot be assumed that threads are executed in the order they are created. \n"});