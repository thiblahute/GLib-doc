fragment_downloaded_cb({"url": "c/unicode.html#g_utf16_to_utf81", "fragment": "g_utf16_to_utf8 \nConvert a string from UTF to UTF The result will be terminated with a byte. \nNote that the input is expected to be already in native endianness an initial byte order mark character is not handled specially. g_convert can be used to convert a byte buffer of UTF data of ambiguous endianess. \nFurther note that this function does not validate the result string it may e.g. include embedded NUL characters. The only validation done by this function is to ensure that the input can be correctly interpreted as UTF i.e. it doesn t contain things unpaired surrogates. \n"});