fragment_downloaded_cb({"url": "c/linked_lists_double.html#parameters1", "fragment": "list \na pointer to a GList this must point to the top of the already sorted list \ndata \nthe data for the new element \nfunc \nthe function to compare elements in the list. It should return a number if the first parameter comes after the second parameter in the sort order. \n"});