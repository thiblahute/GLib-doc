fragment_downloaded_cb({"url": "c/sequence.html#g_sequence_sort_changed1", "fragment": "g_sequence_sort_changed \nMoves the data pointed to a new position as indicated by cmp_func. This function should be called for items in a sequence already sorted according to cmp_func whenever some aspect of an item changes so that cmp_func may return different values for that item. \ncmp_func is called with two items of the seq and user_data. It should return if the items are equal a negative value if the first item comes before the second and a positive value if the second item comes before the first. \n"});