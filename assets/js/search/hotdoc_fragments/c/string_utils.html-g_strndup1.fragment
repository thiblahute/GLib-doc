fragment_downloaded_cb({"url": "c/string_utils.html#g_strndup1", "fragment": "g_strndup \nDuplicates the first n bytes of a string returning a newly allocated buffer n bytes long which will always be nul terminated. If str is less than n bytes long the buffer is padded with nuls. If str is NULL it returns NULL. The returned value should be freed when no longer needed. \nTo copy a number of characters from a UTF encoded string use g_utf8_strncpy instead. \n"});