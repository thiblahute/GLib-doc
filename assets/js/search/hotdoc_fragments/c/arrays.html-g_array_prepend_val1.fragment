fragment_downloaded_cb({"url": "c/arrays.html#g_array_prepend_val1", "fragment": "g_array_prepend_val \nAdds the value on to the start of the array. The array will grow in size automatically if necessary. \nThis operation is slower than g_array_append_val since the existing elements in the array have to be moved to make space for the new element. \ng_array_prepend_val is a macro which uses a reference to the value parameter v. This means that you cannot use it with literal values such as You must use variables. \n"});