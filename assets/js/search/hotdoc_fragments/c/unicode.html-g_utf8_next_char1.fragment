fragment_downloaded_cb({"url": "c/unicode.html#g_utf8_next_char1", "fragment": "g_utf8_next_char \nSkips to the next character in a UTF string. The string must be valid this macro is as fast as possible and has no error checking. You would use this macro to iterate over a string character by character. The macro returns the start of the next UTF character. Before using this macro use g_utf8_validate to validate strings that may contain invalid UTF \n"});