fragment_downloaded_cb({"url": "c/arrays.html#g_array_append_val1", "fragment": "g_array_append_val \nAdds the value on to the end of the array. The array will grow in size automatically if necessary. \ng_array_append_val is a macro which uses a reference to the value parameter v. This means that you cannot use it with literal values such as You must use variables. \n"});