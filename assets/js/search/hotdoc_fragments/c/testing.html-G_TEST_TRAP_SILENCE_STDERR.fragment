fragment_downloaded_cb({"url": "c/testing.html#G_TEST_TRAP_SILENCE_STDERR", "fragment": "Redirect stderr of the test child to dev null so it cannot be observed on the console during test runs. The actual output is still captured though to allow later tests with g_test_trap_assert_stderr. \nG_TEST_TRAP_INHERIT_STDIN \n"});