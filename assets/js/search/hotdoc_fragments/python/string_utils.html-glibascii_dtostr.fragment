fragment_downloaded_cb({"url": "python/string_utils.html#glibascii_dtostr", "fragment": "GLib.ascii_dtostr \nConverts a gdouble to a string using the as decimal point. \nThis function generates enough precision that converting the string back using GLib.ascii_strtod gives the same machine number on machines with IEEE compatible bit doubles It is guaranteed that the size of the resulting string will never be larger than G_ASCII_DTOSTR_BUF_SIZE bytes including the terminating nul character which is always added. \n"});