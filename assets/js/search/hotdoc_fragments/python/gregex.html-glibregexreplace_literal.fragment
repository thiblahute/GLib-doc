fragment_downloaded_cb({"url": "python/gregex.html#glibregexreplace_literal", "fragment": "GLib.Regex.replace_literal \nReplaces all occurrences of the pattern in regex with the replacement text. replacement is replaced literally to include backreferences use GLib.Regex.replace. \nSetting start_position differs from just passing over a shortened string and setting GLib.RegexMatchFlags.notbol in the case of a pattern that begins with any kind of lookbehind assertion such as b \n"});