fragment_downloaded_cb({"url": "python/sequence.html#glibsequenceget_length", "fragment": "GLib.Sequence.get_length \nReturns the length of seq. Note that this method is O h where h is the height of the tree. It is thus more efficient to use GLib.Sequence.is_empty when comparing the length to zero. \n"});