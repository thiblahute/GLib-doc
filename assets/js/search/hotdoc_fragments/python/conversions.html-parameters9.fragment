fragment_downloaded_cb({"url": "python/conversions.html#parameters9", "fragment": "utf8string \na UTF encoded string \nlen \nthe length of the string or if the string is nul terminated Note that some encodings may allow nul bytes to occur inside strings. In that case using for the len parameter is unsafe \n"});