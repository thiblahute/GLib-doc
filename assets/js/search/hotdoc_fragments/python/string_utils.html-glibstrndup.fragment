fragment_downloaded_cb({"url": "python/string_utils.html#glibstrndup", "fragment": "GLib.strndup \nDuplicates the first n bytes of a string returning a newly allocated buffer n bytes long which will always be nul terminated. If str is less than n bytes long the buffer is padded with nuls. If str is None it returns None. The returned value should be freed when no longer needed. \nTo copy a number of characters from a UTF encoded string use GLib.utf8_strncpy instead. \n"});