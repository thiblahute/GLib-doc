fragment_downloaded_cb({"url": "python/conversions.html#returns-a-tuple-made-of3", "fragment": "\nIf the conversion was successful a newly allocated nul terminated string which must be freed with GLib.free. Otherwise None and error will be set. \nbytes_read \nIf the conversion was successful a newly allocated nul terminated string which must be freed with GLib.free. Otherwise None and error will be set. \nbytes_written \nIf the conversion was successful a newly allocated nul terminated string which must be freed with GLib.free. Otherwise None and error will be set. \n"});