fragment_downloaded_cb({"url": "python/conversions.html#glibfilename_from_utf8", "fragment": "GLib.filename_from_utf8 \nConverts a string from UTF to the encoding GLib uses for filenames. Note that on Windows GLib uses UTF for filenames on other platforms this function indirectly depends on the current locale setlocale \n"});