fragment_downloaded_cb({"url": "python/hash_tables.html#g_hash_table_lookup_extended", "fragment": "GLib.hash_table_lookup_extended \nLooks up a key in the GLib.HashTable returning the original key and the associated value and a gboolean which is True if the key was found. This is useful if you need to free the memory allocated for the original key for example before calling GLib.hash_table_remove. \nYou can actually pass None for lookup_key to test whether the None key exists provided the hash and equal functions of hash_table are None safe. \n"});