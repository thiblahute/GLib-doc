fragment_downloaded_cb({"url": "python/arrays_byte.html#glibbytesnew_take", "fragment": "GLib.Bytes.new_take \nCreates a new GLib.Bytes from data. \nAfter this call data belongs to the bytes and may no longer be modified by the caller. GLib.free will be called on data when the bytes is no longer in use. Because of this data must have been created by a call to GLib.malloc GLib.malloc0 or GLib.realloc or by one of the many functions that wrap these calls such as g_new not introspectable GLib.strdup etc \nFor creating GLib.Bytes with memory from other allocators see g_bytes_new_with_free_func not introspectable \ndata may be None if size is \n"});