fragment_downloaded_cb({"url": "python/conversions.html#glibiconvclose", "fragment": "GLib.IConv.close \nSame as the standard UNIX routine iconv_close but may be implemented via libiconv on UNIX flavors that lack a native implementation. Should be called to clean up the conversion descriptor from g_iconv_open not introspectable when you are done converting things. \nGLib provides GLib.convert and GLib.locale_to_utf8 which are likely more convenient than the raw iconv wrappers. \n"});