fragment_downloaded_cb({"url": "python/hash_tables.html#glibhash_table_replace", "fragment": "GLib.hash_table_replace \nInserts a new key and value into a GLib.HashTable similar to GLib.hash_table_insert. The difference is that if the key already exists in the GLib.HashTable it gets replaced by the new key. If you supplied a value_destroy_func when creating the GLib.HashTable the old value is freed using that function. If you supplied a key_destroy_func when creating the GLib.HashTable the old key is freed using that function. \n"});