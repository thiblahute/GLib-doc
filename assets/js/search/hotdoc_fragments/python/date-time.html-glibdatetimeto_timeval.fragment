fragment_downloaded_cb({"url": "python/date-time.html#glibdatetimeto_timeval", "fragment": "GLib.DateTime.to_timeval \nStores the instant in time that datetime represents into tv. \nThe time contained in a GLib.TimeVal is always stored in the form of seconds elapsed since UTC regardless of the time zone associated with datetime. \nOn systems where long is bit ie all bit systems and all Windows systems a GLib.TimeVal is incapable of storing the entire range of values that GLib.DateTime is capable of expressing. On those systems this function returns False to indicate that the time is out of range. \nOn systems where long is bit this function never fails. \n"});