fragment_downloaded_cb({"url": "python/threads.html#glibonce_init_enter", "fragment": "GLib.once_init_enter \nFunction to be called when starting a critical initialization section. The argument location must point to a static initialized variable that will be set to a value other than at the end of the initialization section. In combination with GLib.once_init_leave and the unique address value_location it can be ensured that an initialization section will be executed only once during a program s life time and that concurrent threads are blocked until initialization completed. To be used in constructs like this \n"});