fragment_downloaded_cb({"url": "python/misc_utils.html#glibformat_size", "fragment": "GLib.format_size \nFormats a size for example the size of a file into a human readable string. Sizes are rounded to the nearest size prefix kB MB GB and are displayed rounded to the nearest tenth. E.g. the file size bytes will be converted into the string MB \nThe prefix units base is i.e. kB is bytes \nThis string should be freed with GLib.free when not needed any longer. \nSee GLib.format_size_full for more options about how the size might be formatted. \n"});