fragment_downloaded_cb({"url": "python/testing.html#G_TEST_TRAP_SILENCE_STDOUT", "fragment": "Redirect stdout of the test child to dev null so it cannot be observed on the console during test runs. The actual output is still captured though to allow later tests with g_test_trap_assert_stdout not introspectable \nGLib.TestTrapFlags.silence_stderr \n"});