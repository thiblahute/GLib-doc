fragment_downloaded_cb({"url": "python/main.html#glibpriority_high_idle", "fragment": "GLib.PRIORITY_HIGH_IDLE \nUse this for high priority idle functions. \nGTK uses GLib.PRIORITY_HIGH_IDLE for resizing operations and GLib.PRIORITY_HIGH_IDLE for redrawing operations. This is done to ensure that any pending resizes are processed before any pending redraws so that widgets are not redrawn twice unnecessarily. \n"});