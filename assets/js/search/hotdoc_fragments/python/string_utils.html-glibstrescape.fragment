fragment_downloaded_cb({"url": "python/string_utils.html#glibstrescape", "fragment": "GLib.strescape \nEscapes the special characters b f n r t v and in the string source by inserting a before them. Additionally all characters in the range x01 x1F everything below SPACE and in the range x7F xFF all non ASCII chars are replaced with a followed by their octal representation. Characters supplied in exceptions are not escaped. \nGLib.strcompress does the reverse conversion. \n"});