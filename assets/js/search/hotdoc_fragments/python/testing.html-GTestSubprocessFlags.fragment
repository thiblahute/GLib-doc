fragment_downloaded_cb({"url": "python/testing.html#GTestSubprocessFlags", "fragment": "If this flag is given the child process will inherit the parent s stdin. Otherwise the child s stdin is redirected to dev null. \nGLib.TestSubprocessFlags.stdout \nIf this flag is given the child process will inherit the parent s stdout. Otherwise the child s stdout will not be visible but it will be captured to allow later tests with g_test_trap_assert_stdout not introspectable \nGLib.TestSubprocessFlags.stderr \nIf this flag is given the child process will inherit the parent s stderr. Otherwise the child s stderr will not be visible but it will be captured to allow later tests with g_test_trap_assert_stderr not introspectable \n"});