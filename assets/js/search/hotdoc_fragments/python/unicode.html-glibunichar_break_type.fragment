fragment_downloaded_cb({"url": "python/unicode.html#glibunichar_break_type", "fragment": "GLib.unichar_break_type \nDetermines the break type of c. c should be a Unicode character to derive a character from UTF encoded text use GLib.utf8_get_char The break type is used to find word and line breaks text boundaries Pango implements the Unicode boundary resolution algorithms and normally you would use a function such as pango_break instead of caring about break types yourself. \n"});