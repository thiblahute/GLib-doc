fragment_downloaded_cb({"url": "javascript/memory_slices.html#glibprototypeslice_copy", "fragment": "GLib.prototype.slice_copy \nAllocates a block of memory from the slice allocator and copies block_size bytes into it from mem_block. \nmem_block must be non NULL if block_size is non zero. \n"});