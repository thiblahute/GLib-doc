fragment_downloaded_cb({"url": "javascript/ghostutils.html#g_hostname_is_ascii_encoded", "fragment": "GLib.prototype.hostname_is_ascii_encoded \nTests if hostname contains segments with an ASCII compatible encoding of an Internationalized Domain Name. If this returns true you should decode the hostname with GLib.prototype.hostname_to_unicode before displaying it to the user. \nNote that a hostname might contain a mix of encoded and unencoded segments and so it is possible for GLib.prototype.hostname_is_non_ascii and GLib.prototype.hostname_is_ascii_encoded to both return true for a name. \n"});