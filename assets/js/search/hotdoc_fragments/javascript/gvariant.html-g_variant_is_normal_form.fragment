fragment_downloaded_cb({"url": "javascript/gvariant.html#g_variant_is_normal_form", "fragment": "GLib.Variant.prototype.is_normal_form \nChecks if value is in normal form. \nThe main reason to do this is to detect if a given chunk of serialised data is in normal form load the data into a GLib.Variant using GLib.Variant.prototype.new_from_data and then use this function to check. \nIf value is found to be in normal form then it will be marked as being trusted. If the value was already marked as being trusted then this function will immediately return true. \n"});