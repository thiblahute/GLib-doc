fragment_downloaded_cb({"url": "javascript/conversions.html#returns-a-tuple-made-of3", "fragment": "\nIf the conversion was successful a newly allocated nul terminated string which must be freed with GLib.prototype.free. Otherwise null and error will be set. \nbytes_read \nIf the conversion was successful a newly allocated nul terminated string which must be freed with GLib.prototype.free. Otherwise null and error will be set. \nbytes_written \nIf the conversion was successful a newly allocated nul terminated string which must be freed with GLib.prototype.free. Otherwise null and error will be set. \n"});