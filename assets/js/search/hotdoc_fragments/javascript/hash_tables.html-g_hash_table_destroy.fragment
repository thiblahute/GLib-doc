fragment_downloaded_cb({"url": "javascript/hash_tables.html#g_hash_table_destroy", "fragment": "GLib.prototype.hash_table_destroy \nDestroys all keys and values in the GLib.HashTable and decrements its reference count by If keys and or values are dynamically allocated you should either free them first or create the GLib.HashTable with destroy notifiers using g_hash_table_new_full not introspectable In the latter case the destroy functions you supplied will be called on all keys and values during the destruction phase. \n"});