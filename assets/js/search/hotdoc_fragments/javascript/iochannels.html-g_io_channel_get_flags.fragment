fragment_downloaded_cb({"url": "javascript/iochannels.html#g_io_channel_get_flags", "fragment": "GLib.IOChannel.prototype.get_flags \nGets the current flags for a GLib.IOChannel including read only flags such as GLib.IOFlags.prototype.is_readable. \nThe values of the flags GLib.IOFlags.prototype.is_readable and GLib.IOFlags.prototype.is_writable are cached for internal use by the channel when it is created. If they should change at some later point e.g. partial shutdown of a socket with the UNIX shutdown function the user should immediately call GLib.IOChannel.prototype.get_flags to update the internal values of these flags. \n"});