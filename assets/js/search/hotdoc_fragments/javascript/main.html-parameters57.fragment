fragment_downloaded_cb({"url": "javascript/main.html#parameters57", "fragment": "priority \nthe priority of the idle source. Typically this will be in the range between GLib.PRIORITY_DEFAULT_IDLE and GLib.PRIORITY_HIGH_IDLE. \npid \nprocess to watch. On POSIX the positive pid of a child process. On Windows a handle for a process which doesn t have to be a child \nfunction \nfunction to call \ndata \ndata to pass to function \nnotify \nfunction to call when the idle is removed or null \n"});