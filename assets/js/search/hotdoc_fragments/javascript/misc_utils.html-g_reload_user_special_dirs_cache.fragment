fragment_downloaded_cb({"url": "javascript/misc_utils.html#g_reload_user_special_dirs_cache", "fragment": "GLib.prototype.reload_user_special_dirs_cache \nResets the cache used for GLib.prototype.get_user_special_dir so that the latest on disk version is used. Call this only if you just changed the data on disk yourself. \nDue to threadsafety issues this may cause leaking of strings that were previously returned from GLib.prototype.get_user_special_dir that can t be freed. We ensure to only leak the data for the directories that actually changed value though. \nSince \n"});