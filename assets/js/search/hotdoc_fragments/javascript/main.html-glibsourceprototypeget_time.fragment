fragment_downloaded_cb({"url": "javascript/main.html#glibsourceprototypeget_time", "fragment": "GLib.Source.prototype.get_time \nGets the time to be used when checking this source. The advantage of calling this function over calling GLib.prototype.get_monotonic_time directly is that when checking multiple sources GLib can cache a single value instead of having to repeatedly get the system monotonic time. \nThe time here is the system monotonic time if available or some other reasonable alternative otherwise. See GLib.prototype.get_monotonic_time. \n"});