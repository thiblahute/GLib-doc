fragment_downloaded_cb({"url": "javascript/testing.html#glibprototypetest_add_func", "fragment": "GLib.prototype.test_add_func \nCreate a new test case similar to g_test_create_case not introspectable However the test is assumed to use no fixture and test suites are automatically created on the fly and added to the root fixture based on the slash separated portions of testpath. \nIf testpath includes the component subprocess anywhere in it the test will be skipped by default and only run if explicitly required via the p command line option or GLib.prototype.test_trap_subprocess. \n"});