fragment_downloaded_cb({"url": "javascript/messages.html#g_log_structured_array", "fragment": "GLib.prototype.log_structured_array \nLog a message with structured data. The message will be passed through to the log writer set by the application using GLib.prototype.log_set_writer_func. If the message is fatal i.e. its log level is GLib.LogLevelFlags.prototype.level_error the program will be aborted at the end of this function. \nSee g_log_structured not introspectable for more documentation. \nThis assumes that log_level is already present in fields typically as the PRIORITY field \n"});