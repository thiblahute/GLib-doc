fragment_downloaded_cb({"url": "javascript/gvariant.html#g_variant_n_children", "fragment": "GLib.Variant.prototype.n_children \nDetermines the number of children in a container GLib.Variant instance. This includes variants maybes arrays tuples and dictionary entries. It is an error to call this function on any other type of GLib.Variant. \nFor variants the return value is always For values with maybe types it is always zero or one. For arrays it is the length of the array. For tuples it is the number of tuple items which depends only on the type For dictionary entries it is always \nThis function is O \n"});