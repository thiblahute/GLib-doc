fragment_downloaded_cb({"url": "javascript/ghostutils.html#g_hostname_is_non_ascii", "fragment": "GLib.prototype.hostname_is_non_ascii \nTests if hostname contains Unicode characters. If this returns true you need to encode the hostname with GLib.prototype.hostname_to_ascii before using it in non IDN aware contexts. \nNote that a hostname might contain a mix of encoded and unencoded segments and so it is possible for GLib.prototype.hostname_is_non_ascii and GLib.prototype.hostname_is_ascii_encoded to both return true for a name. \n"});